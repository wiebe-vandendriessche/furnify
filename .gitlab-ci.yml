stages:
  - test

test:
  image: https://gitlab.stud.atlantis.ugent.be/vop2324/furnify2/container_registry/413:latest
  stage: test
  script:
    - cd "$CI_PROJECT_DIR/project"
    - npm ci
    - npm run dev &
    - npm run testViewless
